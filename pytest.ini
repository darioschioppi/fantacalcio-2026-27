[pytest]
testpaths = work/tests
python_files = test_*.py
markers =
    slow: test lento che usa il dataset reale intero (non le fixture) per validare le metriche di training rispetto ai valori di riferimento noti.
